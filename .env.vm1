# =============================================================================
# VM1 Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# Run with: docker-compose --env-file .env.vm1 -f docker-compose.vm1.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Node IP Addresses (REQUIRED - Update these with your actual VM IPs)
# -----------------------------------------------------------------------------
# These must be the actual IP addresses reachable from other nodes
# Do NOT use localhost or 127.0.0.1 in a multi-VM setup

NODE1_IP=192.168.1.101
NODE2_IP=192.168.1.102
NODE3_IP=192.168.1.103

# -----------------------------------------------------------------------------
# Port Configuration
# -----------------------------------------------------------------------------
# These are the ports exposed on the host. Change if you have conflicts.

# etcd ports
ETCD_CLIENT_PORT=2379
ETCD_PEER_PORT=2380

# PostgreSQL port
POSTGRES_PORT=5432

# Patroni REST API port (used by HAProxy for health checks)
PATRONI_API_PORT=8008

# -----------------------------------------------------------------------------
# PostgreSQL Credentials (REQUIRED - Change these for production!)
# -----------------------------------------------------------------------------
# These passwords will be used for PostgreSQL authentication

# Superuser password (used for admin access)
POSTGRES_PASSWORD=your-secure-postgres-password

# Replication user password (used for streaming replication)
REPLICATION_PASSWORD=your-secure-replication-password

# -----------------------------------------------------------------------------
# Optional: Advanced Configuration
# -----------------------------------------------------------------------------
# Uncomment and modify these if needed

# PostgreSQL max connections
# POSTGRES_MAX_CONNECTIONS=200

# Shared buffers (memory)
# POSTGRES_SHARED_BUFFERS=256MB

# Cluster name (must match across all nodes)
# PATRONI_SCOPE=postgres-ha
