# Patroni PostgreSQL HA Cluster Configuration
# Update these values according to your setup

# Database Passwords
POSTGRES_PASSWORD=your-secure-postgres-password
REPLICATION_PASSWORD=your-secure-replication-password

# Consul Configuration (replaces etcd)
# Consul cluster endpoint - will be set by each VM's consul-agent
CONSUL_HOST=consul-agent:8500
CONSUL_DATACENTER=dc1
CONSUL_ACL_TOKEN=

# Alternative Consul hosts if using external Consul cluster
# CONSUL_HOST=192.168.1.100:8500

# Legacy etcd Configuration (deprecated - use Consul instead)
# ETCD_HOSTS=host.docker.internal:32379
